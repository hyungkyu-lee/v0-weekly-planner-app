-- Delete all tasks from the tasks table
-- This will remove all existing tasks for all users
DELETE FROM tasks;

-- Reset any sequences if needed (optional)
-- This ensures a clean slate for the planner
